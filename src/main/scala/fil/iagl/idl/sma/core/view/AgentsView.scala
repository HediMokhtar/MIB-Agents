package fil.iagl.idl.sma.core.view

import fil.iagl.idl.sma.core.model.{Agent, Environment}

import scala.collection.mutable
import scalafx.scene.shape.Shape

trait AgentsView{

  val environment: Environment

  /**
    * Map used to see the correspondance between the agent and the view, the object agent is the key
    */




}
